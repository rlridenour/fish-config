function orgblog-push
    cd ~/sites/orgblog
    quickgit
 	# git --git-dir=$PWD/.git add . # Stage all unstaged files
	# git --git-dir=$PWD/.git commit -a -m "Update orgblog" # Commit files
	# git --git-dir=$PWD/.git push # Push to remote
end
